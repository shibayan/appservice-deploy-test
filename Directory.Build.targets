<Project>
  <Target Name="MakeZipPackage" AfterTargets="AfterPublish">
    <ZipDirectory SourceDirectory="$(ProjectDir)$(publishUrl)" DestinationFile="$(ProjectDir)$(AssemblyName).zip" Overwrite="true" />
  </Target>
</Project>